# Pin exact versions on `pnpm add` / `pnpm install --save` — no `^` or `~`.
# Combined with the manually-pinned package.json entries, this keeps builds
# fully reproducible across machines.
save-exact=true
save-prefix=
