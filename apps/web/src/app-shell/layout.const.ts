/**
 * Layout dimensions shared between the AppBar, the Drawer, and the main
 * content offset. Centralised so all three stay in lockstep when the
 * design changes.
 *
 * Mini-rail (collapsed) width matches `theme.spacing(8)` so icons sit on
 * the 8-pt grid; expanded width is the MUI-recommended `240px` for a
 * persistent navigation drawer.
 */
export const SIDEBAR_WIDTH_EXPANDED_PX = 240;
export const SIDEBAR_WIDTH_COLLAPSED_PX = 64;
export const APP_HEADER_HEIGHT_PX = 64;
